apiVersion: v1
kind: Service
metadata:
  name: spring-devops4-service
spec:
  selector:
    app: spring-devops4
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
